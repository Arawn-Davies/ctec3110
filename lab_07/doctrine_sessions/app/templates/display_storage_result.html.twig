{% extends 'banner.html.twig' %}
{% block content %}
    <div id="page-content-div">
        <h3>{{ page_heading_2 }}</h3>
        <h4>{{ sid_text }}{{ sid }}</h4>
        <p>User name stored was {{ username }}</p>
        <p>The password stored was {{ password }}</p>
        <p>The data was stored in a {{ server_type }}</p>
        <p>{{ storage_result_message }}</p>
        <p class="curr_page"><a href="{{ landing_page }}">Home</a></p>
    </div>
{% endblock %}
